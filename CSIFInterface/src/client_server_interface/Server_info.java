package client_server_interface;

public class Server_info 
{
	public enum mode {WAITING_ROOM, GENERATING, GAME;}
	public enum status {EMPTY, NOT_EMPTY, FULL;}
	public enum waiting_room_status {NOT_READY, READY;}
	
}
